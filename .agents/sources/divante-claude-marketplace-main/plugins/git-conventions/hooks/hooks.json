{
  "description": "Git workflow validation hooks for branch naming and commit messages",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "${CLAUDE_PLUGIN_ROOT}/hooks/validate-branch-name.md",
            "if": "Bash(git checkout -b *)"
          },
          {
            "type": "prompt",
            "prompt": "${CLAUDE_PLUGIN_ROOT}/hooks/validate-branch-name.md",
            "if": "Bash(git branch *)"
          },
          {
            "type": "prompt",
            "prompt": "${CLAUDE_PLUGIN_ROOT}/hooks/validate-branch-name.md",
            "if": "Bash(git switch -c *)"
          },
          {
            "type": "prompt",
            "prompt": "${CLAUDE_PLUGIN_ROOT}/hooks/validate-commit-message.md",
            "if": "Bash(git commit *)"
          }
        ]
      }
    ]
  }
}
