---
status: empty
populated_by: /create-CLAUDE_MD
description: Cross-cutting map of the repository — directory structure, file roles, naming rules
---

# Memory: Directory Structure & File Map

> Regeneratable file. `/create-CLAUDE_MD` rewrites this on every run — do **not** edit by hand for permanent knowledge (use `decisions.md` / `patterns.md` / `domain/{module}.md` for that).
>
> While `status: empty`, skip loading this file — it carries no project knowledge yet.

---

## Source layout

```
{tree of source directories — generated per-project}
```

## Module roles

| Path | Responsibility |
|------|---------------|
| `{path}` | {what lives here} |

## Naming rules

- {file naming convention}
- {symbol naming convention}

## Critical conventions

- {non-obvious rule that affects where new code goes}
